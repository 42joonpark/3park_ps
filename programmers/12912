#include <string>
#include <vector>

using namespace std;

long long solution(int a, int b) {
    long long answer = 0;
    int c = a > b ? b : a;
    int d = a > b ? a : b;
    while (c <= d) {
        answer += c;
        ++c;
    }
    return answer;
}
